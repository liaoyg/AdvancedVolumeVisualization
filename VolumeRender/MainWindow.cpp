#include "MainWindow.h"
#include "GLWidget.h"
#include "VolumeVisEditor.h"
#include <QBoxLayout>
#include <QLabel>

MainWindow::MainWindow(QWidget *parent)
    :QDialog(parent)
{

//    GLWidget w;
//    w.show();
//    VolumeVisEditor vEditor(this);

//    QHBoxLayout *mainLayout = new QHBoxLayout;
//    mainLayout->addWidget(&w,4);
//    mainLayout->addWidget(&vEditor,2);
//    this->setLayout(mainLayout);

//    connect(&vEditor, SIGNAL(ApplyDimToGl(Vec3f)), &w, SLOT(GetDimFromEditor(Vec3f)));
//    connect(&vEditor, SIGNAL(ApplyTF()), &w, SLOT(UpdateTransferFun()));
//    connect(&vEditor, SIGNAL(ApplyTF()), &vEditor, SIGNAL(ApplyDimToGl(Vec3f)));

}
